---
- name: Update packages
  become: true
  ansible.builtin.apt:
    update_cache: true
    upgrade: "yes"
  ignore_errors: true
- name: Ensure vim is installed
  become: true
  ansible.builtin.apt:
    package: vim
    update_cache: true
