---
# - name: Disable IPv6
#   become: true
#   ansible.builtin.copy:
#     src: roles/bootstrap/files/90-disable-ipv6.conf
#     dest: /etc/sysctl.d/90-disable-ipv6.conf

# - name: Add lxc generator for journalctl fix
#   block:
#     - name: Create system generators directory
#       become: true
#       ansible.builtin.file:
#         path: "/etc/systemd/system-generators/lxc"
#         state: directory
#     - name: Copy the generator
#       become: true
#       ansible.builtin.copy:
#         src: roles/bootstrap/files/lxc.generator
#         dest: /etc/systemd/system-generators/lxc/lxc.generator
# - name: Reboot
#   become: true
#   ansible.builtin.reboot:
#     reboot_timeout: 120
- name: Turn off machine to prepare for cloning
  community.proxmox.proxmox:
    api_host: "{{ proxmox_node_ip }}"
    api_user: "{{ proxmox_admin_api_user }}"
    api_password: "{{ proxmox_admin_password }}"
    vmid: "{{ vmid }}"
    state: stopped
